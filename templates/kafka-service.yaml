apiVersion: v1
kind: Service
metadata:
  name: kafka
  namespace: {{ .Values.namespace }}
spec:
  ports:
    - port: 9092            
      targetPort: 9092       
      nodePort: {{ .Values.service.kafka.port }}        
  selector:
    app: kafka
  type: {{ .Values.service.kafka.type }}     
